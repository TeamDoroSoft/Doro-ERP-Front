# Edge external origin. Keep the browser SESSION cookie on the same trusted deployment boundary.
VITE_API_BASE_URL=http://localhost:8080

# Toss Payments test client key only. Never place a secret key in VITE_* variables.
VITE_TOSS_CLIENT_KEY=
